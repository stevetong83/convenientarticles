<h1>User's</h1>
<%	 @users.each do |user| %>
    <p>
    <%=	 link_to user.name, user %> signed up <%=	 user.created_at.strftime("%B %e, %Y") %>
    # of articles: <%=	 user.articles.count %>
    </p>
<%	 end %>
<p>
  <%=	 will_paginate @users %>
</p>

Number of users: <%=	 @users.count %>
